DATABASE_URL="postgresql://bina:bina_dev@localhost:5432/bina_academy?schema=public"

# Must be 128+ hex characters (64 random bytes).
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="<replace-with-128-char-hex-string>"
JWT_EXPIRES_IN="30d"

# Must be 128+ hex characters (64 random bytes).
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
ADMIN_JWT_SECRET="<replace-with-128-char-hex-string>"

OTP_EXPIRES_MINUTES=5
PORT=3000
CORS_ORIGIN="http://localhost:5173"
