{
  "name": "binaa-academy",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api",
    "admin"
  ],
  "scripts": {
    "build": "npm run build --workspace=admin && npm run build --workspace=api",
    "start": "cd api && npx prisma db push && node dist/src/main.js"
  },
  "dependencies": {
    "prisma": "^6.1.0"
  },
  "engines": {
    "node": ">=20.x"
  }
}
