{
  "name": "binaa-academy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd api && npm install --omit=dev --ignore-scripts && npx prisma@6 generate && npx prisma@6 db push && node dist/main"
  },
  "engines": {
    "node": "22.x"
  }
}
