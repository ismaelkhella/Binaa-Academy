{
  "name": "binaa-academy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd api && npm ci && npm run build",
    "start": "cd api && npx prisma db push && node dist/main"
  },
  "engines": {
    "node": "22.x"
  }
}
