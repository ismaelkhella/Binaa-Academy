{
  "name": "binaa-academy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd api && npm install --include=dev && npm run build && npx prisma db push && node dist/main"
  },
  "engines": {
    "node": "22.x"
  }
}
