.tools/
node_modules/
# api/dist is compiled TypeScript — ignore it
api/dist/
# admin/dist is the built React app — track it in git so DigitalOcean gets it on clone
build/
.env
.env.local
*.log
.DS_Store
coverage/
.prisma/
